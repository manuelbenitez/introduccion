// Create four variables (brand, model, production year, owner's name) and assign them a value. Use let and const where appropriate.
// Create a variable to define if a car is new or not new.
// Use the number, boolean and string data type to create the variables.
// If you use a name with more than one word, remember to use the camel case naming convention.
const brand = "Ford";
const model = "Focus";
const productionYear = 2004;
let ownersName = "Manuel Benitez"; //string
let kilometraje = 160000; // number
let isNew = false; // boolean true false

// Ejercicio 2
let name = "mario";
let surname = "Rossi";
let age = 20;
let isMarried = false;
let job = "developer";
let isGraduated = false;

//Ejercicio 3
let x = 5;
let y = 10;
let area = x * y;

console.log(`El area del rectangulo es: ${area}`);
console.log(`El area del rectangulo es: ${x * y}`);

const firstName = "Manuel";
const lastName = "Benitez";

console.log(`El nombre completo de la persona es: ${firstName} ${lastName}`);
